alias yi="yarn install"

alias ys="yarn start"
alias yd="yarn dev"
alias yb="yarn build"
alias yse="yarn serve"

alias hs='history | grep'
alias reload="source ~/.zshrc"
alias rm="trash"

alias myip="curl http://ipecho.net/plain; echo"

alias update='sudo apt-get update && sudo apt-get upgrade'

alias c="clear"
#
## Colorize the ls output ##
alias ls='ls --color=auto'
 
## Use a long listing format ##
alias ll='ls -la'
 
## Show hidden files ##
alias l.='ls -d .* --color=auto'

## get rid of command not found ##
alias cd..='cd ..'

## a quick way to get out of current directory ##
alias ..='cd ..'
alias ...='cd ../../../'
alias ....='cd ../../../../'
alias .....='cd ../../../../'
alias .4='cd ../../../../'
alias .5='cd ../../../../..'

alias ports='netstat -tulanp'

alias rm="trash"

## docker 
alias dcu="docker-compose up"
alias dps="docker ps"

alias todo="code ~/TODO.todo"
alias note="code ~/NOTE.md"

alias t2="tree -L 2"
alias t3="tree -L 3"

alias rm-node-modules="find . -name \"node_modules\" -type d -prune -exec rm -rf '{}' +"

alias rnra="react-native run-android"

alias stats="vnstat"

alias youtube-playlist="youtube-dl  -f 'best[height<=480]' -o '%(playlist)s/%(playlist_index)s - %(title)s.%(ext)s'"

## Sync using rclone

alias syncup="rclone copy ~/SYNC db:/SYNC --exclude node_modules/** -v -L"
alias syncdown="rclone copy db:/SYNC ~/SYNC --exclude node_modules/** -v -L"
## alias yarn="echo 'try using npm'"

## Prisma2

alias p2g="npx prisma2 generate"
alias p2i="npx prisma2 introspect"
alias p2ms="npx prisma2 migrate save --experimental"
alias p2mu="npx prisma2 migrate up --experimental --verbose"

## one-letter shortcuts

alias c="clear"
alias e="exit"
alias r="ranger"
alias t="gnome-terminal ."
alias zr="source ~/.zshrc"

alias spot="ncspot"

# mkdir, cd into it
mkcd () {
mkdir -p "$*"
cd "$*"
}

alias weather="curl wttr.in"
alias pandora="pianobar"

## TMUX
alias t="tmux"
alias ta="t a -t"
alias tls="t ls"
alias tn="t new -t"
alias tks="tmux kill-seesion -t"
alias screen="xset dpms force off"
alias tx="tmuxinator" 
alias txn="tmuxinator new" 
alias txls="tmuxinator list" 

alias net="sudo iftop -i wlp10s0"
